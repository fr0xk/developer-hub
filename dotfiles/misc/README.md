# Additional Configuration Files

This directory contains additional configuration files for the Unixporn light theme setup:

- `inputrc` - Readline configuration for consistent command-line behavior
- `profile` - Environment variables for light-themed applications
- `nanorc` - Light-themed nano editor configuration

These files should be copied to your home directory as hidden files (.inputrc, .profile, .nanorc) to take effect.