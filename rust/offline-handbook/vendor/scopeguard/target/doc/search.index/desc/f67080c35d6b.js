rd_("AoReturns the argument unchanged.000BaCalls <code>U::from(self)</code>.000CgMacro to create a <code>ScopeGuard</code> (always run).EhCreate a new <code>ScopeGuard</code> owning <code>v</code> and with deferred closure \xe2\x80\xa6AiAlways run on scope exit.BdRun on scope exit through unwinding.CiControls in which cases the associated code should be runBnRun on regular scope exit, when not unwinding.Dh<code>ScopeGuard</code> is a scope guard that may own a protected value.Ch\xe2\x80\x9cDefuse\xe2\x80\x9d the guard and extract the value without \xe2\x80\xa6CnA scope guard will run a given closure when it goes out of \xe2\x80\xa6DhReturn <code>true</code> if the guard\xe2\x80\x99s associated code should run \xe2\x80\xa6EhCreate a <code>ScopeGuard</code> that owns <code>v</code> (accessible through deref) \xe2\x80\xa6DhMacro to create a <code>ScopeGuard</code> (run on unwinding from panic).:DfMacro to create a <code>ScopeGuard</code> (run on successful scope \xe2\x80\xa6;")